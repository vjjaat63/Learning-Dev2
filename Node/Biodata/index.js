const bio = require("./bio");  // search in current directory
// const bio = require("bio");  // search in inbuilnpt modules
console.log(bio)

const a = require("os")
const b = require("fs")

console.log(a)
console.log(b)